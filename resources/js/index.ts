import ModalUI from './modules/modal/ModalUI';
import ScrollToTarget from './modules/scrollToTarget/ScrollToTarget';

new ModalUI('.modal', '.js-modal-open', '.js-modal-close');
new ScrollToTarget();
